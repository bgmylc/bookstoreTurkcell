@model bookStoreTurkcell.Models.Genre

@{
    ViewData["Title"] = "Delete";
}

<h1>Sil</h1>

<h3>Türü silmek istediğinize emin misiniz?</h3>
<div>
    <h4>Tür</h4>
    <hr />
    <dl class="row">

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="@Model.ID" />
        <input type="submit" value="Sil" class="btn btn-danger" /> 
    </form>
</div>

<h1></h1>

@if (ViewBag.Message == 0)
{
    <div class="alert alert-warning" role="alert">
        <strong>Oh Hayır!</strong> Kitapları olan bir türü silemezsiniz :( <a asp-action="Index" class="alert-link"> Hadi Listeye dön artık</a>.
    </div>

}
else
{
    <div>
        <a asp-action="Index">Listeye dön</a>
    </div>

}

